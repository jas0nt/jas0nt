### Jas0nᵀ 👨‍💻

```lisp
(let ((name "Jas0nᵀ")
	  (editor "Emacs")
	  (langs '(Java Python)))
  (message (format "Hello,\n\nI am %s, my favorite Editor is %s, and I use %s." name editor langs)))
```

